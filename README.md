# gmmtools
Tools for customised Gaussian Mixture Models (GMMs), built on top of the sklearn's GMM class

It includes utilities with support for:

- marginalisation
- conditioning
- entropy-based conditional mutual information
- Monte Carlo conditional mutual information

## Installation

```bash
pip install git+https://github.com/ankushaggarwal/gmmtools
